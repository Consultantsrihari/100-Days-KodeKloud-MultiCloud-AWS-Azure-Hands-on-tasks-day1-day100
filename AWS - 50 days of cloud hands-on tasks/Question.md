# Day 10 – Attach Elastic IP to EC2 Instance

## Task

The XYZ DevOps team is breaking down the AWS migration into smaller tasks to improve control, reduce risk, and optimize resources.

There is:
- An EC2 instance named **aws-ec2**
- An Elastic IP named **aws-ec2-eip**

Both resources exist in the **us-east-1** region.

### Requirement

Attach the **aws-ec2-eip** Elastic IP to the **aws-ec2** EC2 instance.
