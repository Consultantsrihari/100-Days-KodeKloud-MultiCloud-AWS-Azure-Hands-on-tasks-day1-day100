# Day 1 — Questions: Create Key Pair (AWS Console)

1. Where in the AWS Console do you create or import EC2 key pairs?
2. What steps do you take to create a new key pair and download the private key?
3. Which file formats are available when creating a key pair in the AWS Console? Which format is used by OpenSSH and which is used by PuTTY?
4. How do you set secure permissions on a downloaded `.pem` file before using it to SSH?
5. How do you use the key pair to connect to a Linux EC2 instance? Give an example `ssh` command.
6. How do you use the key pair for a Windows instance to get the administrator password via the console?
7. What happens if you lose the private key file after creation?
8. How do you import an existing public key into AWS?
9. Does deleting a key pair in the console remove the key from EC2 instances that already have it configured? Explain.
10. What security best practice should you follow instead of allowing SSH from anywhere (0.0.0.0/0)?
