# Day 2 — Questions: Create an Azure Virtual Machine

1. What **VM name** and **region** did you use for the lab?
2. Which **image** and **VM size** are required?
3. How do you set the OS disk to **30 GB** and **Standard HDD** in the portal?
4. How do you ensure the NSG allows **SSH (port 22)** for the VM?
5. What Azure CLI command creates the VM with the OS disk size and Standard HDD storage SKU?
6. How do you get the VM public IP and connect via SSH?
7. How do you clean up the VM using Azure CLI?
